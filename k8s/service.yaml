apiVersion: v1
kind: Service

metadata:
  name: margin-agent-service
  namespace: agent-poc

spec:
  selector:
    app: margin-agent

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8000

  type: LoadBalancer